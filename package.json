
{
  "name": "flashcard",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "prebuild": "cd backend && npm install && cd ../root169 && npm install",
    "build": "cd frontend && npm run build",
    "postbuild": "shx mv frontend/build backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "shx": "^0.3.4"
  }
}
